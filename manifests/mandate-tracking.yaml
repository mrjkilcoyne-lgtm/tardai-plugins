apiVersion: apps/v1
kind: Deployment
metadata:
  name: tool-mandate-tracking
  namespace: tardai
  labels:
    app: tool-mandate-tracking
    tardai.tool: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tool-mandate-tracking
  template:
    metadata:
      labels:
        app: tool-mandate-tracking
        tardai.tool: "true"
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
      containers:
        - name: app
          image: ghcr.io/mrjkilcoyne-lgtm/tardai-plugins-mandate-tracking:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: BEARER
              valueFrom:
                secretKeyRef:
                  name: tardai-auth
                  key: bearer
            - name: BUS_BASE
              value: http://tardai-tool-bus.tardai.svc.cluster.local:8000
            - name: ARTEFACT_BASE
              value: http://tardai-artefacts.tardai.svc.cluster.local:8000
            - name: SELF_BASE
              value: http://tool-mandate-tracking.tardai.svc.cluster.local:8000
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 30
          resources:
            requests:
              cpu: 5m
              memory: 5Mi
            limits:
              cpu: 100m
              memory: 32Mi
      imagePullSecrets:
        - name: ghcr-pull
