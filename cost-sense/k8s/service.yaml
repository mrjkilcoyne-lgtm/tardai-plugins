apiVersion: v1
kind: Service
metadata:
  name: tool-cost-sense
  namespace: tardai
spec:
  type: ClusterIP
  selector:
    app: tool-cost-sense
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
