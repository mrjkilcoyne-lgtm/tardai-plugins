apiVersion: v1
kind: Service
metadata:
  name: tool-mempalace-search
  namespace: tardai
spec:
  type: ClusterIP
  selector:
    app: tool-mempalace-search
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
