apiVersion: v1
kind: Service
metadata:
  name: tool-self-artefact-read
  namespace: tardai
spec:
  type: ClusterIP
  selector:
    app: tool-self-artefact-read
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
