apiVersion: v1
kind: Service
metadata:
  name: tool-pod-introspect
  namespace: tardai
spec:
  type: ClusterIP
  selector:
    app: tool-pod-introspect
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
