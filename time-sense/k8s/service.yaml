apiVersion: v1
kind: Service
metadata:
  name: tool-time-sense
  namespace: tardai
spec:
  type: ClusterIP
  selector:
    app: tool-time-sense
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
