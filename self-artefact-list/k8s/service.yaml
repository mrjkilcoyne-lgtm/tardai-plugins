apiVersion: v1
kind: Service
metadata:
  name: tool-self-artefact-list
  namespace: tardai
spec:
  type: ClusterIP
  selector:
    app: tool-self-artefact-list
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
